@page
@model OCFX.Pages.MadminAccess.Exercises.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Exercise[0].ExName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Exercise[0].ExType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Exercise[0].ExGroup)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Exercise) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ExName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExGroup)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
